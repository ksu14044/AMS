# 2026-05-29 (금)

**Phase:** **7** 보고서 검증 · **운영/UX** (가입·조교·학생부)  
**집중 영역:** 보고서 재검증 · 조교 클리닉·접근 · 학생부 · 인사·로그아웃

---

## 완료 (Done)

### 운영 Flyway · 배포 파이프라인
- 운영 DB **Flyway baseline 18** (깨진 `flyway_schema_history` 정리 후 1회)
- **V19/V20 스모크** — push → deploy만으로 V20 적용 확인, 로컬·운영 SQL 결과 동일
- 서버 `.env` GHCR `latest` 권장, `spring-boot-starter-flyway` (SB4)
- 상세: [PROD_FLYWAY_2026-05-29.md](../../db/PROD_FLYWAY_2026-05-29.md), [FLYWAY.md](../../db/FLYWAY.md), [DEPLOY.md](../../../deploy/DEPLOY.md)

### 보고서·기획
- **보고서 샘플 재검증** — 여러 시험·학생 재생성 후 클리닉·종합·ZIP·PDF 확인. 기대대로 반영됨.
- **구간 끝** — `resolvePeriodEnd` = `max(completed_at, test_at)` 코드 반영 확인.

### 인증·가입 (이전 세션 포함·연속)
- 로그인 화면 공개 가입 제거 → **역할별 초대 링크(JWT)** (`SignupInviteService`, 관리자 발급 UI)
- **로그인 유지** — refresh 90일, FE 401 자동 갱신·앱 시작 세션 복구
- 회원가입·학생부 **전화번호 하이픈** (`PhoneNumberFormatter`, `PhoneNumberField`)

### 조교 클리닉
- 조교 홈 **내 이번 주 클리닉** — `GET /me/clinic/weeks/{weekStart}` (담당 반 배정 없이 `clinic_slot.assistant_id` 기준)
- 클리닉 전용 반 접근 분리 — `requireClinicReadableClass` vs `requireReadableClass`
- 홈에서 **인라인 출결** (별도 `/classes` 이동 제거)
- 담임 반 상세 탭 달력 UI (`ClinicStaffCalendar`, `ClinicDayDetailPanel`)

### 학생부
- **BE** `GET /api/v1/students/roster` — 원장·행정(전체), 담임(담임 반 수강생), PENDING 포함
- **전용 페이지** `/admin/students`, `/teacher/students` — 넓은 레이아웃(≈1100px)·**표 형식**
- 홈에는 **학생부 바로가기 카드**만 (`StudentRosterHomeCard`)

### UI 소소
- 선생님·학생 홈 인사 **`{이름}님`** (roleLabel 중복 `님` 제거)
- **학생 홈** 알림 옆 **로그아웃** 버튼

## 진행 중 (In progress)

- (없음)

## 내일·다음 (Next)

- [ ] **개발 서버 시각 검증** — 학생 홈 / 반 상세 / 클리닉 / 보고서·학생부 (≤480px·학생부 wide)
- [ ] 클라이언트 협의 — **행정 = 원장급 운영 권한** 여부
- [ ] Phase 10.2-1: `ClassResponse` — `homeroomTeacherName`, `nextScheduleSummary`
- [ ] (선택) 학생부 행 클릭 → 반 공부기록 드릴다운

## 메모

- 조교2: 담당 반 배정 없이 클리닉 슬롯만 있어도 홈·출결 가능 (반 상세 전체 탭은 불가).
- 학생부 검색: 이름·이메일·전화·반 이름 (`q`).
- **블로커:** 없음.

## 시간 (선택)

| 구분 | 시간 |
|------|------|
| 합계 | — |

---

_퇴근 마무리 · [STATUS.md](../STATUS.md) 반영_
