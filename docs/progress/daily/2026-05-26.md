# 2026-05-26 (화)

**Phase:** **10** 디자인 전면 개편 ✅ (구 운영 → **11**)  
**집중 영역:** Phase 재번호 · 디자인 시스템 적용 · 8개 슬라이스 일괄 완료

---

## 완료 (Done)

- Phase 10 **코드 대조** — 구 운영 롤백 반영 (V17만 잔존)
- **Phase 재번호:** 10 = 프론트 디자인 전면 개편, 11 = 운영(E2E·V17·보안)
- [10-frontend-design.md](../../planning/10-frontend-design.md) 신규 (슬라이스 10-1~10-8)
- [STATUS.md](../STATUS.md) · [ROADMAP.md](../../planning/ROADMAP.md) · [PHASE-SCOPE.md](../PHASE-SCOPE.md) 갱신
- **10-1** 토큰·글로벌·레이아웃 — `design-tokens` 확장, `components.css` 추출, AppLayout 학생 헤더(인사+벨), AuthLayout framing, BottomNav outline+safe-area
- **10-2** 인증·온보딩 — `auth.css` 재작성, focus ring, signup links pill, PendingApproval 목업화
- **10-3** 학생 홈·탭·MY — 과목 큰 카드, 반 카드 정합, `class-list.css` 재작성
- **10-4** 반 상세 — `ClassDetailPage` 헤더(← back) + 탭 pill, 클리닉 슬롯 시간 pill+선택 강조, 보고서 모달 검정 카드, raw hex 전부 토큰화
- **10-5** 교직원·알림 — `admin.css` 재작성·중복 클래스 제거, `AcademyNotices*` 스타일 `class-detail.css` 이관, `NotificationBell` SVG+badge, `Notifications` 필터/카드 토큰화, Staff 반 카드 row 구조
- **10-6** 관리자 UI — `AdminClassesSection` 반 카드 row 구조, 배정 해제 ghost-sm, 폼 select focus ring
- **10-7** 공통 컴포넌트 — 미사용 `App.css`/`src/styles/student.css` 삭제, `NotificationBell` 이 `.ams-icon-btn` 재사용
- **10-8** 반응형·a11y — `.ams-class-detail__tabs` 가로 스크롤(스크롤바 hide·`white-space: nowrap`), `.ams-app__container { overflow-x: hidden }`, focus-visible/터치 영역 점검

## Phase 10.1 — 목업 정합 보강 ✅ (오후 추가 작업)

레퍼런스 두 PNG (`reference/01-home-clinic.png`, `02-clinic-reservation-report.png`) 와 실제 화면 갭을 frontend 만으로 해소.

- **10.1-1** `ClassDetailPage` 헤더 3-col grid — ← / 가운데 반이름+(과목·강의실 부제) / ⋮ disabled placeholder. title overflow ellipsis
- **10.1-2** `ClassHomeSection` "내 클리닉" hero 카드 신설 — 분홍(`primary-light`) 배경 + 시간 pill + 성실도 progress + "공부기록" primary pill 버튼. 기존 "공부기록"·"내 클리닉" 카드 2개를 1개로 통합
- **10.1-3** `ClassReportsSection` 보고서 모달 전면 재구성 — 검정 hero 카드(메타+점수+4 progress, `rate<60`이면 채움=red), 회색 코멘트 카드(read/edit), 통계 카드 3개(흰·흰·accent 분홍), 헤더에 ← 닫기 / 가운데 제목 / ⬇ PDF 아이콘
- **10.1-4** `ClassVideoSection` 영상 카드 — 16:9 검정 컨테이너 + 큰 ▶ SVG 가운데 오버레이, hover 어두움, 인증 상태 메타 인라인 표시. 썸네일 없을 때도 동일한 검정 + ▶
- **10.1-5** `StudentHomePage` 과목 카드 — multi-select 토글 제거, **수강중=검정 / 미수강=회색 surface** 단순화. SVG 책 + 'Ax'·'(x)' glyph. 반 리스트는 모든 수강과목을 과목별 lead 헤더로 그룹핑 노출
- **10.1-6** `utils/lastClass.js` 신규 (localStorage + `ams:last-class-changed` 커스텀 이벤트로 페이지 간 동기화). 학생 홈/클리닉/기록 페이지 카드 클릭 시 기록, 학생 홈에서 매칭 카드를 surface 강조 + 빨간 dot
- **10.1-7** `ClassClinicSection` 학생 모드 전면 재구성 — `WEEKDAY_ORDER` 요일별 그룹 행, 시간 pill 토글(임시 선택), 같은 요일·같은 시간 슬롯 자동 배제, 행 전체 분홍 강조 + "선택됨" 라벨. 하단 풀폭 "예약 저장" 버튼(변경 없으면 disabled). 담임/조교 모드는 기존 즉시-편집 UI 그대로

### UX 결정

| 항목 | 채택 |
|------|------|
| 반 상세 ⋮ 메뉴 | (a) 빈 placeholder (disabled, title="추가 메뉴 (준비 중)") |
| 기록 탭 구조 | (A) 반 리스트 유지 → `?tab=study` 풀스크린 보고서 |
| 클리닉 예약 저장 | (Y) 명시적 저장 버튼 — 임시 선택 후 일괄 commit |

## 저녁 — 성실도 보고서 집계·생성 분리 (Phase 7 운영, DB 변경 없음)

### 원인 정리 (빈 보고서·0%)

- 구간 끝 = 테스트 **점수 저장 시각** `completed_at` (등록 `test_at` 아님)
- 첫 시험 구간 시작 = 수강 **`assigned_at`** (이전 시험 없음 버그 아님)
- 숙제는 `COMPLETED` + `due_at`이 구간 안일 때만 집계
- 채점이 마감(`due_at`)보다 이르면 숙제·영상 등이 0건 → N/A

### 구현

- `TestExamService.saveScores` — 시험 완료만, 보고서 자동 생성 제거
- `DiligenceReportService` — `listGenerationTargets`, `generateReportsForTest`
- API: `GET …/reports/generation-targets`, `POST …/reports/generate/{testId}`
- FE: 보고서 탭 「보고서 생성 / 재생성」, 테스트 탭 안내 문구 변경

### 검증 계획 (5/27)

- 신규 수강생 **강학생** 배정 완료
- 숙제·테스트 마감 **5/27 14:00**, 클리닉 **5/27 14:00** 슬롯
- **14:00 이후** 숙제 완료 처리·클리닉 결과·점수 저장 → 보고서 탭에서 생성
- 기대: `due_at` ≤ `completed_at` 로 숙제·클리닉·테스트 반영

### 추후 검토 (미구현)

- `periodEnd = max(completed_at, test_at)` 등 집계 경계 보정
- 생성 전 조건 게이트(시험 시각 이후만 등)

## 진행 중 (In progress)

- (없음)

## 내일·다음 (Next)

1. **성실도 보고서 실측** — 강학생, 5/27 14:00 이후 완료·채점·보고서 생성 → 항목·기간·종합 점수 확인
2. 개발 서버 시각 검증 (모바일 폭 ≤480px, hero·클리닉 UI)
3. **Phase 10.2 (BE):** `ClassResponse` — `homeroomTeacherName`, `nextScheduleSummary`
4. **Phase 11-2:** V17 아카이브 API
5. **Phase 11-1:** Playwright E2E 골격

## 메모

- `ams_backend/target/` surefire 리포트는 롤백 전 빌드 잔재 — `src` 기준으로 판단
- Phase 9·8 문서는 기존 완료 표기 유지 (본일 미검증)
- 디자인 시스템: [10-frontend-design.md](../../planning/10-frontend-design.md) + [DESIGN_SYSTEM.md](../../design/DESIGN_SYSTEM.md)
- 보고서 트리거: 기획서 §11-3「결과 입력 시 자동」→ **보고서 탭 수동 생성**으로 운영 변경 (ROADMAP 문구는 검증 후 정리)

---

_퇴근 전 [STATUS.md](../STATUS.md) 반영 — 2026-05-26_
